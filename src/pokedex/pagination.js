const pokedex = require('../../');

function PagesPokedex(ArrayPokedex = []) {
    const arrayPages = [];
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }
}